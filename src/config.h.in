#pragma once

#define GLINT_VERSION_STRING "${VERSION}"
#define GLINT_VERSION_MAJOR ${VERSION_MAJOR}
#define GLINT_VERSION_MINOR ${VERSION_MINOR}
#define GLINT_VERSION_PATCH ${VERSION_ALTER}
#define GLINT_VERSION_BUILD ${VERSION_BUILD}
#define GLINT_GIT_HASH_FULL "${GIT_COMMIT_LONG}"
#define GLINT_GIT_HASH_SHORT "${GIT_COMMIT}"
#define GLINT_GIT_BRANCH "${GIT_BRANCH}"
