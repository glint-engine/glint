{
  "devDependencies": {
    "@biomejs/biome": "2.3.10",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "typecheck:muen": "tsc -p muen2djs/tsconfig.json --noEmit",
    "typecheck:examples": "tsc -p examples/tsconfig.json --noEmit",
    "lint": "biome lint",
    "format": "biome format --write"
  }
}
